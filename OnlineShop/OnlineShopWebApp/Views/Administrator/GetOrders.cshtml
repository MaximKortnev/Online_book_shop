@model List<OrderData>

<div class="container-fluid">
    <div class="row">
        @await Html.PartialAsync("AdminPanelNavigation")

        <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
            <h2 class="text-center mr-5">Список заказов</h2>
            <table class="table">
                <thead>
                    <tr>
                        <th scope="col">№</th>
                        <th scope="col">Сумма</th>
                        <th scope="col">Статус</th>
                        <th scope="col">Пользователь</th>
                        <th scope="col">Дата</th>
                        <th scope="col"></th>
                    </tr>
                </thead>
                <tbody>
                    @for (int i = 0; i < Model.Count; i++)
                    {
                        <tr>
                            <td>@(i + 1)</td>
                            <td>@Model[i].TotalCost</td>
                            <td>@Model[i].Status</td>
                            <td>@Model[i].Email</td>
                            <td>@Model[i].Data</td>
                            <td>
                                <a style="color:white" class="btn btn-sm btn-info"  asp-action="Info" asp-controller="AdminOrders">Инфо</a>
                            </td>
                            <td>
                                <a style="color:white" class="btn btn-sm btn-info" asp-route-orderId="@Model[i].Id" asp-action="Delete" asp-controller="AdminProducts">Удалить</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </main>
    </div>
</div>